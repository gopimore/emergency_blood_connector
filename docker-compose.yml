services:
  mongo:
    image: mongo:7
    container_name: ebc-mongo
    ports:
      - '27017:27017'
    volumes:
      - ebc_mongo_data:/data/db
    restart: unless-stopped

volumes:
  ebc_mongo_data:

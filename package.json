{
  "name": "emergency-blood-connector",
  "version": "1.0.0",
  "private": true,
  "description": "Emergency Blood Connector — monorepo root",
  "scripts": {
    "dev": "npm run dev:all",
    "dev:all": "concurrently -n backend,frontend -c red,blue \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start": "npm run build --prefix frontend && npm run start --prefix backend",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "setup": "node scripts/setup.js",
    "db:up": "docker compose up -d mongo",
    "db:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "license": "ISC"
}
